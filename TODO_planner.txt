unit tests
validation / error handling client-side and server-side. check if item id matches slot.
save / load configuration
improve layout: stats on the right side of the form, improve table layout

read stats from wowhead planner rather than building my own
